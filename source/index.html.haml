---
layout: main
pageable: true
per_page: 10
---

- content_for :nav do
  = partial 'partials/nav'

%h4.center What I've Seen Recently

- if paginate && num_pages > 1
  %p
    Page #{page_number} of #{num_pages}
  - if prev_page
    %p= link_to 'Previous page', prev_page

%ul.posts
  - blog.articles[0...4].each do |article|
    %li.post
      = link_to article, class: 'post-link' do
        .post-image
          %img{:src => "images/#{article.data.image}"}
        .post-heading
          %h1.post-title
            = article.title
          %h5.post-meta-info
            = article.data.location
          %h5.post-meta-info
            = article.date.strftime('%b %e')

- if paginate
  - if next_page
    %p= link_to 'Next page', next_page

- content_for :footer_main do
  = partial 'partials/footer_main'
