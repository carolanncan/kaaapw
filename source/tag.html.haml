---
layout: main
pageable: true
per_page: 12
---

%h4.center #{tagname}
- if paginate && num_pages > 1
  %p
    Page #{page_number} of #{num_pages}
  - if prev_page
    %p= link_to 'back', prev_page

%ul.posts
  - page_articles.each_with_index do |article, i|
    %li.post
      = link_to article do
        .post-image
          %img{:src => "../images/#{article.data.image}"}
        .post-heading
          %h5.date
            = article.date.strftime('%b %e')
          %h1
            = article.title
          %h5.location
            = article.data.location
- if paginate
  - if next_page
    %p= link_to 'next', next_page

- content_for :footer_main do
  = partial 'partials/footer_main'
